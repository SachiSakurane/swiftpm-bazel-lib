import XCTest
@testable import swiftpm_bazel_lib

final class swiftpm_bazel_libTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(swiftpm_bazel_lib().text, "Hello, World!")
    }
}
